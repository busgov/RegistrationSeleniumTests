namespace RegistrationSeleniumTests
{
    internal enum ByType
    {
        XPath,
        Id,
        ClassName,
        CssSelector,
        Name,
        TagName,
        LinkText,
        PartialLinkText
    }
}
